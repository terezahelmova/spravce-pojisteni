for x in rnage(10,0,-1):
    if x == 1:
        tvar = "zelená lahev"
    elif 1 < x < 5:
        tvar = "zelené lahve"
    else:
        tvar = "zelených lahví"
    print(x, tvar, "stojí na stole a jedna spadne")
input()
